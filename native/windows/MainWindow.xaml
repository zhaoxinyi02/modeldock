<Window
    x:Class="ModelDock.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModelDock"
    Title="ModelDock">

    <Grid x:Name="RootGrid">
        <NavigationView x:Name="Navigation" PaneDisplayMode="Left" IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False" OpenPaneLength="230" SelectionChanged="Navigation_SelectionChanged">
            <NavigationView.PaneHeader>
                <StackPanel Orientation="Horizontal" Spacing="10" Margin="4,10,0,14">
                    <Image Source="Assets/app_logo_light.png" Width="30" Height="30"/>
                    <TextBlock Text="ModelDock" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </NavigationView.PaneHeader>
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="HomeNav" Content="首页" Tag="home" IsSelected="True">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Home"/></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="配置管理" Tag="models">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Library"/></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="回退点" Tag="restores">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Clock"/></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="设置" Tag="settings">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Setting"/></NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Grid>
                <Grid x:Name="HomePage" Padding="28,22,28,28">
                    <ScrollViewer>
                        <StackPanel Spacing="18">
                            <Grid>
                                <StackPanel>
                                    <TextBlock Text="Codex Gateway" Style="{StaticResource PageTitleStyle}"/>
                                    <TextBlock Text="官方订阅与第三方模型的 Windows 原生控制中心" Style="{StaticResource SecondaryTextStyle}" Margin="0,5,0,0"/>
                                </StackPanel>
                                <Button HorizontalAlignment="Right" VerticalAlignment="Center" Click="Refresh_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="8"><SymbolIcon Symbol="Refresh"/><TextBlock Text="刷新"/></StackPanel>
                                </Button>
                            </Grid>

                            <Border Style="{StaticResource CardStyle}">
                                <Grid ColumnDefinitions="*,Auto">
                                    <StackPanel Spacing="5">
                                        <TextBlock x:Name="ModeTitle" Text="正在读取" FontSize="22" FontWeight="SemiBold"/>
                                        <TextBlock x:Name="ModeDescription" Style="{StaticResource SecondaryTextStyle}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                    <Border x:Name="ModeBadge" Grid.Column="1" Background="{ThemeResource AccentFillColorSecondaryBrush}" CornerRadius="16" Padding="14,7">
                                        <TextBlock x:Name="ModeBadgeText" FontWeight="SemiBold"/>
                                    </Border>
                                </Grid>
                            </Border>

                            <Grid ColumnDefinitions="*,*" ColumnSpacing="16">
                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel Spacing="12">
                                        <TextBlock Text="本地网关" Style="{StaticResource CardTitleStyle}"/>
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <FontIcon x:Name="GatewayStatusIcon" Glyph="&#xE73E;"/>
                                            <TextBlock x:Name="GatewayStatusText" FontWeight="SemiBold"/>
                                        </StackPanel>
                                        <TextBlock x:Name="GatewayUrlText" Style="{StaticResource SecondaryTextStyle}"/>
                                        <TextBlock x:Name="GatewayModelsText" Style="{StaticResource SecondaryTextStyle}"/>
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <Button Content="启动" Tag="start" Click="GatewayAction_Click"/>
                                            <Button Content="停止" Tag="stop" Click="GatewayAction_Click"/>
                                            <Button Content="重启" Tag="restart" Click="GatewayAction_Click"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                                    <StackPanel Spacing="12">
                                        <TextBlock Text="Codex Desktop" Style="{StaticResource CardTitleStyle}"/>
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <FontIcon x:Name="CodexStatusIcon" Glyph="&#xE73E;"/>
                                            <TextBlock x:Name="CodexStatusText" FontWeight="SemiBold"/>
                                        </StackPanel>
                                        <TextBlock x:Name="CodexProcessesText" Style="{StaticResource SecondaryTextStyle}"/>
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <Button Content="打开" Tag="start" Click="CodexAction_Click"/>
                                            <Button Content="停止" Tag="stop" Click="CodexAction_Click"/>
                                            <Button Content="重启" Tag="restart" Click="CodexAction_Click"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel Spacing="10">
                                    <TextBlock Text="官方账号" Style="{StaticResource CardTitleStyle}"/>
                                    <Grid ColumnDefinitions="130,*" RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="8">
                                        <TextBlock Text="邮箱" Style="{StaticResource SecondaryTextStyle}"/><TextBlock x:Name="AccountEmailText" Grid.Column="1"/>
                                        <TextBlock Grid.Row="1" Text="套餐" Style="{StaticResource SecondaryTextStyle}"/><TextBlock x:Name="AccountPlanText" Grid.Row="1" Grid.Column="1"/>
                                        <TextBlock Grid.Row="2" Text="凭据更新时间" Style="{StaticResource SecondaryTextStyle}"/><TextBlock x:Name="AccountRefreshText" Grid.Row="2" Grid.Column="1"/>
                                        <TextBlock Grid.Row="3" Text="第三方模型" Style="{StaticResource SecondaryTextStyle}"/><TextBlock x:Name="ThirdPartyCountText" Grid.Row="3" Grid.Column="1"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>

                <Grid x:Name="ModelsPage" Visibility="Collapsed" Padding="28,22,28,24" RowDefinitions="Auto,*,Auto">
                    <StackPanel>
                        <TextBlock Text="配置管理" Style="{StaticResource PageTitleStyle}"/>
                        <TextBlock Text="双击模型进行编辑；序号固定，移动只改变 Codex 中的排列顺序" Style="{StaticResource SecondaryTextStyle}" Margin="0,5,0,16"/>
                    </StackPanel>
                    <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
                        <Grid RowDefinitions="42,*">
                            <Grid Padding="12,0" Background="{ThemeResource SubtleFillColorSecondaryBrush}" ColumnDefinitions="48,1.25*,1*,1*,1.5*,90,90">
                                <TextBlock Text="序号" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="名称" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="2" Text="上游模型" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="3" Text="类型" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="4" Text="URL" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="5" Text="上下文" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="6" Text="最大输出" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            </Grid>
                            <ListView x:Name="ModelsList" Grid.Row="1" SelectionMode="Single" DoubleTapped="ModelsList_DoubleTapped">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="8,9" ColumnDefinitions="48,1.25*,1*,1*,1.5*,90,90">
                                            <TextBlock Text="{Binding Number}" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding DisplayName}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="2" Text="{Binding Upstream}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="3" Text="{Binding TypeLabel}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="4" Text="{Binding BaseUrl}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="5" Text="{Binding ContextText}" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="6" Text="{Binding OutputText}" VerticalAlignment="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Border>
                    <Grid Grid.Row="2" Margin="0,14,0,0" ColumnDefinitions="Auto,Auto,Auto,Auto,*,Auto" ColumnSpacing="8">
                        <Button Click="AddModel_Click"><StackPanel Orientation="Horizontal" Spacing="7"><SymbolIcon Symbol="Add"/><TextBlock Text="添加模型"/></StackPanel></Button>
                        <Button Grid.Column="1" Click="EditModel_Click"><StackPanel Orientation="Horizontal" Spacing="7"><SymbolIcon Symbol="Edit"/><TextBlock Text="编辑"/></StackPanel></Button>
                        <Button Grid.Column="2" Content="上移" Tag="-1" Click="MoveModel_Click"/>
                        <Button Grid.Column="3" Content="下移" Tag="1" Click="MoveModel_Click"/>
                        <Button Grid.Column="5" Click="DeleteModel_Click"><StackPanel Orientation="Horizontal" Spacing="7"><SymbolIcon Symbol="Delete"/><TextBlock Text="删除"/></StackPanel></Button>
                    </Grid>
                </Grid>

                <Grid x:Name="RestoresPage" Visibility="Collapsed" Padding="28,22,28,24" RowDefinitions="Auto,*,Auto">
                    <StackPanel>
                        <TextBlock Text="回退点" Style="{StaticResource PageTitleStyle}"/>
                        <TextBlock Text="恢复网关、Codex provider、模型目录和登录配置" Style="{StaticResource SecondaryTextStyle}" Margin="0,5,0,16"/>
                    </StackPanel>
                    <ListView x:Name="RestoresList" Grid.Row="1" SelectionMode="Single">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,8" Padding="16">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <StackPanel Spacing="4">
                                            <StackPanel Orientation="Horizontal" Spacing="9"><TextBlock Text="{Binding Name}" FontWeight="SemiBold"/><TextBlock Text="{Binding KindLabel}" Style="{StaticResource SecondaryTextStyle}"/></StackPanel>
                                            <TextBlock Text="{Binding Notes}" Style="{StaticResource SecondaryTextStyle}" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                            <TextBlock Text="{Binding CreatedAt}"/>
                                            <TextBlock Text="{Binding Items}" HorizontalAlignment="Right" Style="{StaticResource SecondaryTextStyle}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="0,14,0,0">
                        <Button Click="CreateRestore_Click"><StackPanel Orientation="Horizontal" Spacing="7"><SymbolIcon Symbol="Add"/><TextBlock Text="创建手动回退点"/></StackPanel></Button>
                        <Button Click="Restore_Click">恢复所选回退点</Button>
                    </StackPanel>
                </Grid>

                <Grid x:Name="SettingsPage" Visibility="Collapsed" Padding="28,22,28,28">
                    <ScrollViewer>
                        <StackPanel Spacing="16">
                            <TextBlock Text="设置" Style="{StaticResource PageTitleStyle}"/>
                            <TextBlock Text="模式、官方凭据、会话保护与网关" Style="{StaticResource SecondaryTextStyle}" Margin="0,-11,0,0"/>
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Codex 登录状态" Style="{StaticResource CardTitleStyle}"/>
                                    <TextBlock x:Name="LoginStatusText" TextWrapping="Wrap"/>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Button Content="重新登录 / 刷新凭据" Click="Login_Click"/>
                                        <Button Content="打开官方 Usage" Click="Usage_Click"/>
                                    </StackPanel>
                                    <ComboBox x:Name="ModeCombo" Header="运行模式" HorizontalAlignment="Stretch" SelectionChanged="ModeCombo_SelectionChanged">
                                        <ComboBoxItem Content="纯官方订阅" Tag="pure_official"/>
                                        <ComboBoxItem Content="官方订阅 + 第三方 API" Tag="official_plus_api"/>
                                        <ComboBoxItem Content="纯 API + 第三方模型" Tag="api_only"/>
                                    </ComboBox>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="会话保护" Style="{StaticResource CardTitleStyle}"/>
                                    <TextBlock x:Name="ConversationText"/>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Button Content="创建会话索引快照" Click="ConversationSnapshot_Click"/>
                                        <Button Content="修复并统一会话列表" Click="ConversationRepair_Click"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="开机自启与端口" Style="{StaticResource CardTitleStyle}"/>
                                    <ToggleSwitch x:Name="AutostartToggle" Header="登录后自动启动本地网关" Toggled="AutostartToggle_Toggled"/>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <NumberBox x:Name="PortBox" Header="端口" Minimum="1" Maximum="65535" Width="150" SpinButtonPlacementMode="Inline"/>
                                        <Button Content="应用端口并重启" VerticalAlignment="Bottom" Click="ApplyPort_Click"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel Spacing="7">
                                    <TextBlock Text="关于" Style="{StaticResource CardTitleStyle}"/>
                                    <TextBlock x:Name="AboutVersionText"/>
                                    <TextBlock Text="WinUI 3 · Windows App SDK · Mica" Style="{StaticResource SecondaryTextStyle}"/>
                                    <TextBlock Text="本地网关 CLIProxyAPI 7.2.92" Style="{StaticResource SecondaryTextStyle}"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>

                <ProgressRing x:Name="BusyRing" IsActive="False" Width="44" Height="44" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <InfoBar x:Name="NoticeBar" IsOpen="False" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="20" IsClosable="True"/>
            </Grid>
        </NavigationView>
    </Grid>
</Window>
